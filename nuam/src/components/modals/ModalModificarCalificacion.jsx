export default function ModalModificarCalificacion({ onClose }) {
  return (
    <div style={{ background: "#fff", padding: 20, border: "1px solid #ccc" }}>
      <h3>Modificar Calificación</h3>

      <p>Contenido por definir.</p>

      <button onClick={onClose}>Cerrar</button>
    </div>
  );
}
